# dms-greeter's RPM creates the 'greeter' user only via a %pre useradd, which
# bootc deployments don't persist. This ensures systemd-sysusers recreates it
# on every deploy so greetd can launch dms-greeter under it.
u greeter 953 "System Greeter" /var/lib/greeter /bin/bash
