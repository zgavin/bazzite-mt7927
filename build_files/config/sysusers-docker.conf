# docker-ce's rpm creates the docker group via a %post groupadd, which bootc
# deployments don't persist (only rpm-owned files survive). Without this,
# /etc/group on the deployed system loses the entry and anyone needing to
# access /var/run/docker.sock has to recreate it manually.
g docker 955
