---
name: Build Bluefin images
on:
  pull_request:
    branches:
      - main
  schedule:
    - cron: '05 10 * * *'  # 10:05am UTC everyday
  push:
    branches:
      - main
    paths-ignore:
      - '**/README.md'
  workflow_dispatch:

jobs:
  build_push:
    strategy:
      fail-fast: false
      matrix:
        base_image:
          - bluefin
          - bluefin-nvidia-open
          - bluefin-dx
          - bluefin-dx-nvidia-open
        tag:
          - stable
          - gts
    uses: ./.github/workflows/build-image.yml
    with:
      base_image: ${{ matrix.base_image }}
      tag: ${{ matrix.tag }}
      image_desc: "Bluefin with MediaTek MT7927/MT6639 WiFi 7 and Bluetooth kernel module patches"
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write
